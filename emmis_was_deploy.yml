---
- name: Download EAR file from Jenkins to /tmp on Docker Host
  hosts: 10.112.30.40
  become: yes
  tasks:
    - name: Download EAR file
      get_url:
        url: "http://mdcmwp-jenkins.mcd.ifox.loc:8080/job/eMMIS-DXCD/lastSuccessfulBuild/"
        dest: /tmp/medicaid_EAR6.ear
        mode: '0644'
